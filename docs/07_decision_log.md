# 07. 결정 이력 (Decision Log)

> 무엇을 / 왜 / 대안. 새 결정은 맨 위에 추가. (사주 프로젝트는 D-1xx 사용)

| ID | 날짜 | 결정 | 이유 | 대안(기각) |
|---|---|---|---|---|
| D-117 | 2026-06-27 | **200+ 실사용 시나리오 테스트(서브에이전트)** → 🔴 0건 확인 후 🟡 반영: 데일리 럭키 **매일 변주**(명식 고정 → 도움오행/오늘 일진오행 시드 택1), 헤드라인 풀 5/밴드, 궁합 띠라벨 이중출력 수정, 궁합 공유문구 관계별, 연운 에러문구 중복 제거, 이름 보완 예시 다양화 | 핵심 차별점 "매일 데일리"의 최대 약점 = 럭키 색/방향 영구 고정 + 헤드라인 연속일 반복(재방문 매력 저하). 실사용 품질 8/10 | 럭키 고정 유지(며칠이면 질림), 콘텐츠 소폭(반복感) |
| D-116 | 2026-06-27 | **출품 강화(콘텐츠 깊이+정확성 감수+적대적 QA) 완료.** 핵심: **입력 숫자 제약(min/max/int)을 Zod 스키마에서 제거**하고 `z.coerce.number()`로 받아, 범위 검증을 엔진(`birthToProfile`/`computeChart`/`computeYearlyFortune`)의 한글 `RangeError`로 이전. + 엔진 영문에러 한글화, PII 숫자열 프로필코드 차단, 이름 10자 캡, 잘못된 날짜/달 친절 안내, 택일 제목중복 수정 | QA 발견: Zod 범위 제약 위반 시 **MCP SDK가 핸들러 전에 raw `-32602`+Zod JSON을 클라이언트로 누출**(친절 카드·칩·면책 전무) = 데모/심사 킬러. 스키마 제약은 핸들러 try/catch로 못 잡음 → 제약을 걷고 엔진서 친절 처리해야 함. 정확성 감수는 오류 0건(합격) | 핸들러서 잡기(불가 — SDK가 먼저 가로챔), 제약 유지(친절 폴백 무력) |
| D-115 | 2026-06-27 | **택일 툴 + 날씨 보강 출시** — `findAuspiciousDate`(보조 유틸: 명식 충 회피+손없는날+목적별 가중) / `getTodayFortune`에 Open-Meteo 무키 날씨·미세먼지 best-effort(한글 도시 로컬 좌표표, 위치 시·2.5s·실패 무해) | 리서치 차별화(택일=PlayMCP 경쟁자 0, "한눈에 다름")·사용자 요구(날씨 체감). 둘 다 무키 → KC 단순 배포·안정성 유지. 택일은 리텐션 약해 **메인 아님**(사용자 지시) | 택일 메인화(리텐션 약함), 날씨 키 API(KC 키 문제 재발), 지오코딩만(한글명 실패) |
| D-114 | 2026-06-27 | **차별화 실행 패키지 확정** — (1) **모든 결과에 공유 카드 + 바이럴 훅**(귀여운 카드 UI + 공유받은 사람용 "나도 보기" CTA + 서비스명; `lib/share.ts`, `ok(body,choices,share?)`), (2) **데일리 처방 강화**(럭키 음식·시간대=개운법, 무키), (3) 포지셔닝 = **"사주로 오늘을 운영하는 데일리 비서"**(1FATE 딥명리·사주봄 '쉬운 분석'과 차별), (4) 차기 신툴 **택일(findAuspiciousDate)** 채택 | 사용자 지시(모든 서비스 공유 가능·귀여운 UI·수신자 유입 장치) + 리서치(사주봄이 '쉬운 사주' 선점 → 우리는 처방/공유/택일로 비켜섬; Co-Star 공유 공식=바이럴→공개투표). 전부 무키·자체데이터 | 분석 결과만 제공(클론 위험·투표 열세), 공유 장치 없음(바이럴 모수 손실) |
| D-113 | 2026-06-27 | **"빈 땅" 전제 폐기 → 차별화 전략 확정.** PlayMCP에 사주 MCP 이미 존재(1FATE '정통 명리학': 격국·용신·신살48·12운성·궁합·오늘기운 / '사주봄': 오늘·띠별·행운시간). 우리 포지션 = **정통/전문이 아니라 "재미·공유·매일 습관·대중 접근성"**: 사주 MBTI 16유형·데일리 럭키 키트+카톡 데일리·공유 궁합 카드·칩 여정·단계적 진입. 정통 명리(격국·용신·신살 다수)는 **깊게 안 감**(그쪽은 1FATE 영역) | 노션 마켓 정독서 경쟁자 확인(빈 땅 아님). 심사정책 "명칭·문구·출력만 바꾼 클론 불인정" → 차별화는 생존조건. 우리 시그니처가 심사축(창의성·편의성·안정성)+공개투표와 정합 | 정통 명리 깊이로 정면승부(1FATE에 열세·클론위험), 차별화 없이 진행(반려·투표 열세) |
| D-112 | 2026-06-27 | **"외부 키 0개" 고수 철회** — 완성도·차별화에 도움 되면 외부 API/키를 추가한다(키 0은 기본 선호일 뿐 제약 아님). 단 **KC는 환경변수 입력란 없음** → 키 추가 시 GitHub Secrets→비공개 ghcr 이미지 빌드주입(docs/09) 비용을 감수. 키 1개라도 추가하면 Git 소스 빌드(public)→컨테이너 이미지(비공개) 배포로 전환 필요 | 사용자 지시: 가치가 우선, 키 0을 선호·고수할 필요 없음. 단순함은 좋지만 더 나은 서비스가 먼저 | 키 0 도그마 유지(기능 제약), 키를 repo 커밋(안전분류기 차단·위반) |
| D-111 | 2026-06-27 | **사주 16유형 4축 매핑** 확정 (음양=일간 음양 / 기세=신강신약[비겁+인성+1 vs 식상+재성+관성] / 성향=식상 vs 관성 / 기온=목화 vs 금수) + **데일리 키트 산출 규칙** 확정 (테마=오늘 일진 천간의 일간 대비 십신카테고리; 점수=테마 기본값 ± 오행균형 보정 ± 일자 시드; 럭키=부족오행 또는 인성오행→색/숫자/방향/아이템; do/don'ts=테마+오행+직업) | "미결정 TODO" 2건 해소. 명식에서 바로 산출되고 16유형이 전부 정의됨. 결정론·순수함수 → 테스트·재현 가능(idempotent: 유형 true / 데일리 false) | 십신 최빈값 단일 분류(정보량 부족), 난수 기반(재현 불가) |
| D-110 | 2026-06-27 | **무저장 프로필 코드** 채택(양력생일+경도+선택 성별/위치/직업 인코딩, stateless·무로그인·무DB) · 이메일 발송 드롭 · 리텐션 = "매일 바뀌는 럭키 키트 + 티저 칩('내일 미리보기/이번주 흐름') + **클라이언트(에이전트) 스케줄링→카톡 '나와의 채팅방' 전달**" | MCP는 요청-응답이라 서버가 스스로 푸시 불가(이메일/카톡/푸시 모두). 카카오 발표상 OpenClaw·ChatGPT·Claude가 "매일 아침 ~ 카톡으로 보내줘"로 우리 툴을 자동 호출·전달 가능 → **서버는 "스케줄 친화적 단일 호출"만 잘하면 됨.** 저장·이메일·백엔드 불필요 = 무키·stateless 강점 유지. 카톡(본선 Kakao Tools)이 핵심 소비 surface | 서버 DB 저장+로그인/OAuth(개인정보처리방침·반려위험·무의존 강점 상실), 이메일 발송(별도 백엔드·이메일키·정보통신망법 수신동의/거부) |
| D-109 | 2026-06-27 | 데일리 키트에 **위치 기반 날씨·미세먼지**를 **키 없는 Open-Meteo로** best-effort 보강(선택 기능) + 직업 맥락 do/don'ts 선택 입력 | 사용자 요청(체감·리텐션). 기상청/에어코리아 키는 KC 환경변수 없음·p99·egress IP 함정 재발 → "키 0개" 강점 훼손. **Open-Meteo는 무키**라 강점 유지(외부 호출 1개만 추가, 실패해도 운세는 정상 출력). ⚠️ 데이터 CC BY 4.0(출처표기 필수)·**비상업 한정**(광고·결제 없는 무료 출품이라 부합, 향후 상업화 시 재검토) | 기상청/에어코리아(키 필요·함정 재발), 날씨 미포함(체감↓) |
| D-108 | 2026-06-27 | 제품 격상: **단계적 진입**(띠/생년→생년월일→선택 시각, 시 모름 모드) + **시그니처 4종**(① 사주 MBTI 유형 ② 공유용 궁합 카드 ③ 데일리 럭키 키트 ④ 이름풀이/작명) + 데일리 키트 구성(점수·키워드 애정/재물/일/건강·오늘의 럭키 색/숫자/아이템·do&don'ts). **코어 툴 6개 확정**(computeSajuChart·getTodayFortune·getCompatibility·analyzePersonality·getYearlyFortune·interpretName), 타로는 보조/후순위 | 사용자 브리프 = "대다수 임팩트·편의·중독·바이럴". 단계적 진입=진입장벽↓(대중도달), 데일리 키트=매일 재방문(중독), MBTI유형·궁합카드=공유(바이럴→공개투표 모수↑). 명식계산+자체해석이 해자라 반려 회피 | 풀입력만(진입장벽↑), 타로 코어 포함(초점 분산), 7+툴(명확성 점수↓ — kpass 교훈) |
| D-107 | 2026-06-26 | **1번째 작품(Korea Trip Concierge) 실전 경험을 docs/09로 이식** + 스택을 검증 구성(TS+Node22, MCP SDK v1.29, express5 stateless)으로 확정 | 같은 대회 동일 절차를 한 번 통과한 자산 → 시행착오 반복 방지. KC 환경변수 없음·linux/amd64·등록절차·네이밍 빌드게이트 등 공식 가이드에 없는 함정 확보 | 처음부터 새로 시행착오(시간 낭비) |
| D-106 | 2026-06-26 | **Saju는 Git 소스 빌드(public)로 배포, 외부 API 키 의존 0 유지** | 1번째 작품 최대 난관=KC에 환경변수 입력란 없음(키 주입 위해 ghcr 비공개 우회 필요). Saju는 키가 없어 이 문제를 원천 회피 = 안정성·단순성 강점 | 외부 운세 API 도입(키 주입 고생 + 라이선스·반려 위험) |
| D-104 | 2026-06-26 | **AGPL 코드(orrery·Swiss Ephemeris) 사용 금지, 개념만 참조** | AGPL은 서비스 제공 시 전체 소스 공개 강제 → 서버 전염 | orrery 직접 의존(전염), 코드 복붙(위반) |
| D-103 | 2026-06-26 | 선택지 UX는 예선 "응답 내 칩"(A) 100%, elicitation(B)은 본선 옵션 | PlayMCP AI채팅 TextContent만 보장 | 예선부터 elicitation 의존(미동작 위험) |
| D-102 | 2026-06-26 | 명식 엔진 = **manseryeok-js (MIT)** 확정 | MIT·KASI 데이터·활발한 유지보수·DB불필요·TS(스택통일) | sajupy(성숙도 낮음), orrery(AGPL) |
| D-101 | 2026-06-26 | 2번째 제출작 = **한국형 사주 운세 MCP** | 해외 소비자 MCP는 점성술뿐 → 한국화 시 국내 첫 주자, 대중성·중독성·공유성↑ | 가격 디코더/구독 다이어트(데이터 자동수집 불가로 기각) |

## 미결정 (TODO)
- [x] ~~chartCode 요약 포맷~~ → **프로필 코드 `SC1|양력생일|시각|경도|성별|위치|직업` 확정**(D-110, `src/engine/profile.ts`). 개인식별·6종 아님, 무저장.
- [x] ~~사주 16유형 매핑 규칙~~ → **D-111 확정** (`engine/personality.ts`)
- [x] ~~데일리 럭키 키트 산출 규칙~~ → **D-111 확정** (`engine/daily.ts`)
- [ ] 타로 포함 범위 + 카드 이미지 저작권(텍스트만 vs 퍼블릭도메인 이미지)
- [ ] 이름풀이 획수·오행 규칙 세부 확정
- [ ] 신살 포함 개수(과하면 복잡·불안) 

## 기각된 아이디어 (참고)
- 장보기 가격 디코더: 타 커머스 가격 데이터 합법 확보 불가(크롤링=약관위반·심사반려, API 미보편).
- 구독 다이어트: 흩어진 구독을 자동 수집하려면 마이데이터/금융인증 필요(MCP 불가, 개인정보 반려).
- → 공통 교훈: **"흩어진 개인/타사 데이터 자동수집"이 핵심가치인 아이디어는 본 플랫폼에서 막힘.**
